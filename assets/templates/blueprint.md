# 项目蓝图：<项目名>

> 版本：v0.1 ｜ 日期：YYYY-MM-DD ｜ 状态：**draft（待用户评审）** → approved
> 本文档是项目的唯一架构真相源。实现与本文冲突时：要么改代码，要么更新本文并补 ADR——不允许静默漂移。

## 1. 背景与目标（来自 requirements.md §1）

- 一句话：<…>
- 成功标准：<…>
- 完整需求见 [requirements.md](requirements.md)；本文只放摘要：MUST 需求 <N> 条、SHOULD <N> 条、COULD <N> 条、不做 <N> 项。

## 2. 规模估算（Phase 2 产出）

```
假设：DAU ___，人均 ___ 请求，峰值系数 ___，读:写 = ___
推算：平均 QPS ≈ ___，峰值 QPS ≈ ___（其中写 ≈ ___）
存储：<核心表 __ GB + 内容类 __ GB/年 → 存放位置>
带宽：峰值 ___ ≈ ___
结论：档次 L_。无/有定时洪峰场景：<说明>
```

**难点标注**（驱动 §7 的针对性设计）：
- <如：秒杀热点行、全文搜索、大文件、报表聚合…>

## 3. 技术栈（Phase 3 产出，版本锁定）

| 层 | 选型 | 版本 | 理由 |
|---|---|---|---|
| 前端 | React | 18 | 团队熟悉 |
| 后端 | Node + Fastify | 22 LTS | 与前端同语言 |
| 数据库 | PostgreSQL | 16 | 默认推荐，档次 L2 够用 |
| 缓存 | Redis | 7 | 读压力热点 |
| 部署 | Docker Compose + 云 PG | - | L2 标准配置 |

关键选型的完整权衡见 ADR 索引（§10）。

## 4. 架构设计（Phase 4 产出）

**风格**：<模块化单体>，理由：<一句话>。备选方案权衡表：

| 方案 | 优点 | 代价 | 结论 |
|---|---|---|---|
| 模块化单体 | 部署简单、事务简单 | 单体扩容上限 | ✅ 选它 |
| 微服务 | 独立扩展 | 分布式全套税，团队规模不支持 | 否，见 ADR-0002 |

**模块划分**：

| 模块 | 职责（一句话） | 依赖 | 承载的需求 |
|---|---|---|---|
| auth | 注册/登录/会话 | shared | REQ-010~012 |
| article | 文章 CRUD/发布 | shared | REQ-001~005 |
| comment | 评论/审核 | article(公开接口), shared | REQ-006~009 |

**架构图**（L1/L2 必放，Mermaid 源码直接贴）：

```mermaid
graph LR
    …
```

**前端设计（界面与体验）**——页面是前端需求的基本单位，页面清单是前端需求的对账基础：

| 页面 | 路由 | 承载 REQ | 关键组件与数据依赖 |
|---|---|---|---|
| <首页> | / | REQ-xxx | <卡片流/列表等> |

- 导航/信息架构：<主导航结构，一句话或小图>
- 关键交互流程：<核心路径的界面流转，需要时贴 mermaid>
- 组件库/设计系统：<与 §3 一致；依据"界面与体验"维度的偏好与对标站点>
- 响应式 / SEO / 首屏性能：<策略一句话>
- 界面状态约定：加载（骨架屏）、空态、错误页（404/500）
- 视觉稿：<有品牌/演示要求时，填关键页面 HTML 静态稿的路径与用户确认状态；无则写"文字线框即可">

## 5. 数据设计（Phase 5 产出）

- ER 图（Mermaid erDiagram）：<贴图>
- 存储选型：<主库/缓存/对象存储，为什么够用>
- 关键实体的状态机：<订单/文章等，Mermaid stateDiagram>
- 迁移策略：<工具、编号递增、只增不改>

## 6. API 设计（Phase 5 产出）

完整清单（含鉴权与 REQ 追溯；错误码表见附录或独立文件）：

| 方法+路径 | 用途 | 鉴权 | 实现的 REQ |
|---|---|---|---|
| POST /api/v1/articles | 创建文章 | 作者 | REQ-003 |

权限模型：<角色 × 资源能力表；RBAC 表结构说明>

## 7. 质量与运维设计（Phase 6 产出）

### 7.1 安全设计（不可裁剪）
- 认证：<方案与库>；授权：<逐接口检查机制>
- 输入校验：<schema 方案>；密钥管理：<.env + .gitignore + .env.example>

### 7.2 性能设计（针对 §2 难点标注逐条给方案）
- <难点 1> → <方案>
- 缓存策略：<key 设计、TTL、cache-aside>
- N+1 预防与慢查询日志：<约定>

### 7.3 失效模式分析
| 组件故障 | 用户看到 | 恢复手段 | 结论 |
|---|---|---|---|
| 数据库挂 | 500 | 重启 + 备份恢复（RTO 1h） | 接受 |
| 支付回调丢失 | 订单卡"支付中" | 对账定时任务每 10 分钟 | 设计覆盖 |

### 7.4 测试策略
- 每条 MUST 需求 ≥ 1 条自动化测试；质量门禁：lint + type check + 全量测试 + 冒烟
- E2E 核心链路：<3-8 条路径清单>

### 7.5 部署与运维
- 环境：dev / prod（+staging）；部署方式：<…>
- CI：<流水线三步>；监控：<存活探针 + Sentry + 磁盘/连接数告警>
- 备份：<策略> + 恢复演练记录：<日期>

## 8. 风险与未决项

| 风险 | 影响 | 缓解 |
|---|---|---|
| <第三方 X 额度限制> | <影响> | <缓解/接受> |
| 待澄清 Q-003 | <影响> | <负责人/日期> |

## 9. 演进路线（现在的哪些决定为将来铺路）

- 触发条件 → 动作：如"单库 CPU 持续 > 60% → 读写分离（应用已做读写路由抽象）"
- <模块化单体 → 拆服务的前提与顺序>

## 10. 关键决策索引（ADR）

| ADR | 标题 | 状态 |
|---|---|---|
| [ADR-0001](decisions/ADR-0001-xxx.md) | <标题> | accepted |

## 11. 里程碑总览（详见 plan.md）

| 里程碑 | 内容 | 可演示物 | 目标日期 |
|---|---|---|---|
| M1 | 骨架+认证 | 能注册登录的空应用 | <日期> |
